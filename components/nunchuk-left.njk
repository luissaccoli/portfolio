{#
  Nunchuk template (image left, text right)
  Usage:
    {% from "components/nunchuk-left.njk" import nunchuk_left %}
    {{ nunchuk_left('/assets/example.png', 'Alt text', 'Caption HTML or text') }}

  Parameters:
    - src: image path
    - alt: image alt text
    - caption: caption text (HTML allowed)
    - image_class: additional classes for the image
#}

{% macro nunchuk_left(src, alt, caption, image_class='') -%}
<div class="nunchuk nunchuk--image-left">
  <img class="nunchuk__image {{ image_class }}" src="{{ src }}" alt="{{ alt }}">
  <div class="nunchuk__content">
    <p class="nunchuk__text">{{ caption | safe }}</p>
  </div>
</div>
{%- endmacro %}
